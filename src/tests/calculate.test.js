import calculate from '../logic/calculate';

describe('Testing calute function', () => {
  test('Testing input: AC ', () => {
    // Arrange obj, buttonName
    const obj = {
      total: '20',
      next: '10',
      operation: '+',
    };

    // Act

    // Assert
  });
});
